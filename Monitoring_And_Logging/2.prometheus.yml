global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'ml-model-exporter'
    scrape_interval: 5s
    static_configs:
      - targets: ['localhost:5002']
        labels:
          app: 'heart-failure-prediction'